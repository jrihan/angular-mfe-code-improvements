name: "1 - [FEAT] Build & PR"

on:
  push:
    branches:
      - 'feature**'

jobs:
  app:
    uses: itau-corp/itau-up2-nested-reusable-nodejs-s3/.github/workflows/feature.yml@v2-stk-wk
    secrets: inherit
