name: "4 - [PROD] Promote & Deploy"

on:
  pull_request:
    types: edited
    branches: main

jobs:
  app:
    uses: itau-corp/itau-up2-nested-reusable-nodejs-s3/.github/workflows/production.yml@v2-stk-wk
    secrets: inherit
