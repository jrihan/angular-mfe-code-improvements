name: "8 - Cancel and Rollback Deployment"

on:
  issues:
    types: opened

jobs:
  app:
    uses: itau-corp/itau-up2-nested-reusable-nodejs-s3/.github/workflows/rollback.yml@v2-stk-wk
    secrets: inherit
